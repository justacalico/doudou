import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function HomePage() {
  return (
    <PagePlaceholder
      title="Home"
      description="Feature-parity port in progress. This shell mirrors Flutter navigation and keeps all data access through Rust commands."
    />
  );
}
