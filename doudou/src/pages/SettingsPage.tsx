import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Server management, playback tuning, theme, and privacy-first options will be ported with the same behavior."
    />
  );
}
