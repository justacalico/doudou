import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function SearchPage() {
  return (
    <PagePlaceholder
      title="Search"
      description="Real-time multi-entity search will use Rust commands only, with recent and contextual search support."
    />
  );
}
