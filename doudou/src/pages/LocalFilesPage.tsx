import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function LocalFilesPage() {
  return (
    <PagePlaceholder
      title="Local Files"
      description="Folder scanning, metadata parsing, and artwork fallback logic will be ported from Flutter local service."
    />
  );
}
