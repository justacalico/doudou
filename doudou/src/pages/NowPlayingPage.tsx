import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function NowPlayingPage() {
  return (
    <PagePlaceholder
      title="Now Playing"
      description="Full-screen mobile now-playing with queue and expanded controls will mirror Flutter overlays."
    />
  );
}
