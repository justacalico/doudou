import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function DownloadsPage() {
  return (
    <PagePlaceholder
      title="Downloads"
      description="Offline download manager with progress, pause/resume, and manual deletion is in the backend command plan."
    />
  );
}
