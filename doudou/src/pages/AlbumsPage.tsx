import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function AlbumsPage() {
  return (
    <PagePlaceholder
      title="Albums"
      description="Album grid/list parity target with sort and filter controls from Flutter."
    />
  );
}
