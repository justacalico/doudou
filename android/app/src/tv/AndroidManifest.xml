<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <!-- TV-specific features -->
    <uses-feature android:name="android.software.leanback" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Remove phone-only permissions merged from main manifest -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" tools:node="remove" />
    <uses-permission android:name="androidx.car.app.MEDIA_TEMPLATES" tools:node="remove" />

    <!-- Remove watch feature merged from main manifest -->
    <uses-feature android:name="android.hardware.type.watch" tools:node="remove" />
    <!-- Remove car feature merged from main manifest -->
    <uses-feature android:name="android.software.car.app.library" tools:node="remove" />

    <application
        android:label="Doudou"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <meta-data
              android:name="io.flutter.embedding.android.EnableImpeller"
              android:value="true" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Audio service is needed for media playback on TV -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
        <intent-filter>
            <action android:name="android.media.browse.MediaBrowserService" />
        </intent-filter>
        </service>

        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
        <intent-filter>
            <action android:name="android.intent.action.MEDIA_BUTTON" />
        </intent-filter>
        </receiver>

        <!-- Remove Android Auto service and car meta-data -->
        <service android:name="com.oguzhnatly.flutter_android_auto.AndroidAutoService"
            tools:node="remove" />
        <service android:name=".DoudouWearableListenerService"
            tools:node="remove" />
        <meta-data android:name="com.google.android.gms.car.application"
            tools:node="remove" />
        <meta-data android:name="androidx.car.app.minCarApiLevel"
            tools:node="remove" />

        <!-- Remove YouTube Music deep link intent-filters (YTM disabled on TV) -->
        <!-- These are handled by removing the intent-filters from the activity -->
    </application>
</manifest>
